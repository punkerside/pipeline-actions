name: Gitflow

on:
  pull_request:
    branches:
      - release
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: make base