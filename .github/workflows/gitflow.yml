name: Gitflow

on:
  pull_request:
    branches:
      - release
      - master

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Security Tests
      run: |
           make base
           make snyk_code
           make build
           make snyk_test